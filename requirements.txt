# Core — Plano B (sempre necessário)
networkx>=3.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pyvis>=0.3.2

# Opcional — Plano A (GraphSAGE). Instale só se for usar a GNN de verdade:
#   pip install torch torch-geometric
# torch>=2.0
# torch-geometric>=2.4
