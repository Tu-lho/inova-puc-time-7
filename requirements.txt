# Core — Demo sintética (sempre necessário)
networkx>=3.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
pyvis>=0.3.2

# Necessário para a segunda etapa (GNN com datasets reais)
pyyaml>=6.0

# Opcional — Plano A (GraphSAGE) na demo, e necessário para a segunda etapa
# (treino da GNN com datasets reais / transfer learning):
#   pip install torch torch-geometric   # CPU; p/ GPU veja docs/SEGUNDA-ETAPA.md
# torch>=2.0
# torch-geometric>=2.4
# kaggle>=1.6        # opcional: baixar os datasets via CLI
